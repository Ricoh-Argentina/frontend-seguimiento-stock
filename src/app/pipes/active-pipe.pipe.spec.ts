import { ActivePipePipe } from './active-pipe.pipe';

describe('ActivePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ActivePipePipe();
    expect(pipe).toBeTruthy();
  });
});
